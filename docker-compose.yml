
services:
  postgres:
    container_name: job_vacancies_postgres
    image: postgres:16.10-alpine
    ports:
      - ${POSTGRES_PORT}
    environment:
      - POSTGRES_USER=${POSTGRES_USER}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - POSTGRES_DB=${POSTGRES_DB}